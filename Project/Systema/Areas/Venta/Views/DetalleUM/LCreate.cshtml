

<div class="modal-header">
    <h3 class="modal-title">Precio Cliente</h3>
</div>
<div class="modal-body">
    <form class="form-horizontal">


        <div class="form-group">
            <label class="col-md-3 control-label">Precio Venta</label>
            <div class="col-md-9">
                <div class="input-group">
                    <span class="input-group-addon">S/.</span>
                    <input type="number" min="0" name="valor1" id="valor1" ng-model="CNew.Precios" ng-required="true" onkeypress="return soloNumeros(event)">
                </div>
            </div>
        </div>



        <div class="form-group">
            <label class="col-md-3 control-label">Cliente</label>

            <div class="col-md-6">
                <div class="input-icon right">
                    <select ng-model="CNew.ID_Socio" class="form-control"
                            data-ng-options="a.ID_Socio as a.Nombres_Socio for a in CSocioList" ng-required="true"></select>
                </div>
            </div>
        </div>


        <div class="form-group">
            <label class="col-md-3 control-label">Fecha de Vigencia</label>
            <div class="col-md-9">
                <input type="text" class="form-control input-inline input-medium" datepicker-popup="dd/MM/yyyy"
                       ng-model="CNew.Fecha_vigen" value="{{CNew.Fchope | date:'dd/MM/yyyy'}}"
                       is-open="opened" min-date="minDate" max-date="'2019-06-22'" datepicker-options="dateOptions"
                       ng-required="true" close-text="Close" />
                <span class=""><button type="button" class="btn btn-default" ng-click="open($event)" ng-disabled="disabled"><i class="glyphicon glyphicon-calendar"></i></button></span>
            </div>
        </div>



    </form>

</div>
<div class="modal-footer">
    <button class="btn btn-primary" ng-click="Cok()" ng-disabled="!CNew.Precios || !CNew.ID_Socio">Aceptar</button>
    <button class="btn btn-warning" ng-click="cancel()">cancelar</button>
</div>




<script src="http://code.jquery.com/jquery-1.9.1.min.js"></script>
<script>
    (function ($) {
        $.fn.currencyInput = function () {
            this.each(function () {
                var wrapper = $("<div class='currency-input' />");
                $(this).wrap(wrapper);
                $(this).before("<span class='currency-symbol'>$</span>");
                $(this).change(function () {
                    var min = parseFloat($(this).attr("min"));
                    var max = parseFloat($(this).attr("max"));
                    var value = this.valueAsNumber;
                    if (value < min)
                        value = min;
                    else if (value > max)
                        value = max;
                    $(this).val(value.toFixed(2));
                });
            });
        };
    })(jQuery);

    $(document).ready(function () {
        $('input.currency').currencyInput();
    });
</script>

<script type="text/javascript">
    // Solo permite ingresar numeros.
    function soloNumeros(e) {
        var key = window.Event ? e.which : e.keyCode
        return ((key >= 48 && key <= 57) || (key == 46))
    }
</script>




<script language="JavaScript" type="text/JavaScript">
    function envia(obj) {
        if (
        (parseInt(document.getElementById("valor1").value, 10) < parseInt(document.getElementById("valor2").value, 10))
        && !isNaN(parseInt(document.getElementById("valor1").value, 10))
        && !isNaN(parseInt(document.getElementById("valor2").value, 10))
        ) {
            obj.submit();
        } else {
            alert("El valor 2 no es mayor que el valor 1");
        }
    }
</script>







